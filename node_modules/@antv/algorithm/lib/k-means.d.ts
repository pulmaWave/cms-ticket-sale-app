import { NodeConfig } from './types';
/**
 *  k-means算法 根据节点属性之间的欧氏距离将节点聚类为K个簇
 * @param originNodes 节点集合
 * @param k 质心（聚类中心）个数
 */
declare const kMeans: (originNodes: NodeConfig[], k?: number, involvedKeys?: string[], uninvolvedKeys?: string[], propertyKey?: string) => NodeConfig[][];
export default kMeans;
